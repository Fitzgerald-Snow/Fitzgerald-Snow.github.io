# Mac 和 Windows 系统自动生成的文件
.DS_Store
Thumbs.db

# Log 文件
npm-debug.log*
yarn-error.log
yarn-debug.log

# 依赖文件夹 (最重要的一条！)
# 这个文件夹由 `npm install` 自动生成，体积巨大，绝不能上传
/node_modules

# 编译生成的结果文件夹
# 这个文件夹由 `npm run build` 生成，理论上也可以不上传，
# 但对于简单的 GitHub Pages 项目，将其上传可以省去配置自动构建的麻烦。
# 如果你希望上传构建结果，可以注释掉下面这行。
/dist

# 编辑器配置文件
.vscode/
.idea/

# Firebase服务账户密钥（敏感信息，绝不能上传）
firebase-service-account.json

# 临时测试文件
test-static.html
